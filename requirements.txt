# Quantum-Ai-PCB-Builder Python Dependencies
# Core application dependencies

# No external dependencies required for the base implementation
# The application uses Python standard library only

# Optional dependencies for production use:
# openai>=1.0.0          # For OpenAI GPT integration
# anthropic>=0.7.0       # For Claude integration
# pika>=1.3.0            # For RabbitMQ integration
# redis>=5.0.0           # For Redis result backend
# uvicorn>=0.24.0        # For async API server
# fastapi>=0.104.0       # For production API framework
